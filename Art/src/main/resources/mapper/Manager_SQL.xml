<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Admin">

<select id="PList" parameterType="hashmap" resultType="hashmap">
	SELECT P.POST_NO, P.CATEGORY_NO, P.TITLE, U.USER_NICKNAME AS NICKNAME,
			U.USER_ID, U.NAME, U.USER_ID,
			TO_CHAR(P.REGISTER_DATE, 'YY-MM-DD') AS R_DATE, P.VIEWS
	FROM POST P INNER JOIN USERS U
	            ON P.USER_NO = U.USER_NO
	WHERE P.DEL = 1 AND P.VISIBILITY = 0
</select>








</mapper>